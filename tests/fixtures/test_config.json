{
  "test_config": {
    "version": "0.1.0",
    "expected_modules": ["nn", "optim", "autograd", "cuda"],
    "performance_thresholds": {
      "import_time_ms": 2000,
      "function_call_us": 1000,
      "memory_growth_objects": 100
    }
  },
  "cuda_config": {
    "min_compute_capability": "3.5",
    "expected_functions": ["is_available", "device_count"],
    "timeout_seconds": 10
  }
}
